# Step2 Modify YAML in Git

Fork the example repository to your own GitHub account so you can make commits.

Edit one of the YAML files in the plain directory, for example change a container image tag, a label, or a ConfigMap value.

Commit and push the change:
```bash
git add .
git commit -m update
