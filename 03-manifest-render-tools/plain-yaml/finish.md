### ✅ WELL DONE !
Completed lab module: plain-yaml Plain YAML Manifest Source

📝 Recap Notes
1. Plain YAML is the simplest Application source: Argo CD reads YAML files directly and applies them to the cluster.
2. Git changes are detected by repo-server and compared against the live cluster state.
3. Deleting a YAML file in Git will remove the corresponding cluster resource only when prune is enabled.

Next lab module: `kustomize-overlay`.
