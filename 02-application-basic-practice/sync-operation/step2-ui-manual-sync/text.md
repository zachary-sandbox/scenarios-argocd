# Step2 Trigger manual sync from Web UI
1. Enter application detail page on Web-UI
2. Click **SYNC** button
3. Keep default sync options, click **SYNCHRONIZE** confirm.

Observe status transition: OutOfSync → Syncing → Synced.
The manually-edited deployment replica count gets reverted back to git manifest value.
