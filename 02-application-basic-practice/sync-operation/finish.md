### ✅ WELL DONE !
Completed lab module: sync-operation manual sync hands-on

📝 Recap Notes
1. Manual cluster edits will make application enter OutOfSync state (without selfHeal).
2. Sync action applies git desired manifests onto cluster, reverts manual modifications.
3. Sync can be triggered both via Web UI button or via `argocd app sync` cli imperative call.

Next lab module: `auto-sync-selfheal` learn automated sync policy, self-heal and prune.
